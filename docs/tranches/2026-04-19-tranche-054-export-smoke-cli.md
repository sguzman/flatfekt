# Tranche 054 — Export smoke test + CLI polish

Goal: add an automated (feature-gated) GPU export smoke test and improve export CLI output ergonomics.

Source roadmap: `docs/roadmaps/export-roadmap.md`

## Roadmap items executed
- [x] Add feature-gated GPU export integration smoke test (bake + export 0.1s to temp dir)
- [x] Improve export CLI output (paths + summary) and support `--out <dir>` for `export-mp4`
