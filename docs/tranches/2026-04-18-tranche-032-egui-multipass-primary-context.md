# Tranche 032 (2026-04-18) — fix viewer egui multipass scheduling (1 item)

Selected roadmap items (exactly 1):

- [x] UI (Egui): Ensure control panel runs inside `EguiPrimaryContextPass` multipass loop (avoid “No fonts available…” panic)
