# Tranche 028 (2026-04-17) — `--x11` unix backend override (1 item)

Selected roadmap items (exactly 1):

- [ ] Core (Platform): Allow X11 as an opt-in unix backend (`--x11` flag; Wayland remains default)
- [x] Core (Platform): Allow X11 as an opt-in unix backend (`--x11` flag; Wayland remains default)
